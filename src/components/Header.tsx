import styled from "@emotion/styled";

const Header = () => {
  return <StyledHeader>s</StyledHeader>;
};

export default Header;

const StyledHeader = styled.header`
  width: 100%;
  height: 72px;
  border: 1px solid black;
`;
